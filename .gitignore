node_modules/
dist/
*.log
.DS_Store
.env
.env.*
!.env.example
coverage/

# session-local editor state
.specstory/
.cursorindexingignore

# spike throwaway output
spikes/**/out/
.gstack/
spikes/spike2-extension/ext-dir/
problems/
extension/dist/
traces/
gaps/
feedback/
.ide-data/

# build artifacts
*.tsbuildinfo
shared/dist/
.ide-data/
targets/
feedback/

# target-less practice reps (same class of data as targets/ and problems/)
reps/
reps.json

# launch-origin metric (the composer-landing falsifier)
launches.jsonl
# willingness-to-pay probe (paywall.ts) — carries user ids and emails
paywall.jsonl
# subscription entitlement (billing.ts) — customer + subscription ids
subscriptions.jsonl
sessions-live.json
.ide-data/

# vendored LeetCode dataset (~120MB; fetched by `cli.ts lc fetch`, pinned in lc-source.ts)
datasets/

# per-user topic ledgers (same class of data as gaps/)
topics/

# welcome-email send log (welcome.ts). Carries beta user emails; the
# authoritative dedupe is the welcome_emails table in Postgres, not this.
outreach/

# git worktrees parked under .claude/ (full repo checkouts, never commit)
.claude/worktrees/

# qa harness runtime droppings (qa/README.md)
qa/.shim-log.jsonl
qa/.shim-control.json

# per-user daily throttle ledgers (app.ts dailyUserCount) — carry user ids
planner-turns.jsonl
clarify-runs.jsonl
